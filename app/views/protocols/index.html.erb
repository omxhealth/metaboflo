<h1>All Protocols</h1>

<table class="list">
  <tr>
    <th>Name</th>
    <th>Version</th>
		<th colspan="3">Actions</th>
  </tr>

<% @protocols.each do |protocol| %>
<tr class="<%= cycle('odd', 'even') %>">
    <td><%= protocol.name %></td>
    <td><%= protocol.version %></td>
    <td><%= show_link protocol %></td>
    <td><%= edit_link edit_protocol_path(protocol) %></td>
    <td><%= destroy_link protocol %></td>
  </tr>
<% end %>
</table>

<br />

<%= new_link 'protocol', new_protocol_path %>
