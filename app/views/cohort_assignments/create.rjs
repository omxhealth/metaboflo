if @successful
  page.replace_html "assignable-associations", :partial => 'cohort_assignment', :collection => @cohort.cohort_assignments, :locals => { :from => 'cohort' }
  page.replace_html 'flash', "<p class=\"notice\">Added animal to cohort successfully</p>"
else
  page.replace_html 'flash', "<p class=\"error\">Error adding animal to cohort</p>"
end

# page.visual_effect :highlight, "drug_categorization-association-#{@drug_categorization.id}", :duration => 1      
