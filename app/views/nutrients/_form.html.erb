<%= render_form_errors(@nutrient) %>

<%= knoxy_form_for(@nutrient) do |f| %>
  <fieldset>
    <%= f.text_field :name %>
  </fieldset>

  <div class="field-no-label actions">
    <%= f.submit %>
  </div>
<% end -%>
