<h1>Editing task</h1>

<% form_for(@task) do |form| %>
	<%= render :partial => form %>
	
  <p>
    <%= form.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @task %> |
<%= link_to 'Back', tasks_path %>
