<h1>New ingredient</h1>

<%= labelled_tabular_form_for(@ingredient) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => { :form => f } %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>
