<h1>Editing ingredient</h1>

<%= labelled_tabular_form_for(@ingredient) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => { :form => f } %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= show_link @ingredient %>
