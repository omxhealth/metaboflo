<h1>Editing experiment</h1>

<%= labelled_tabular_form_for([@sample, @experiment]) do |form| %>
  <%= render :partial => form %>
  <p>
    <%= form.submit "Update" %>
  </p>
<% end %>

<%= show_link [@sample, @experiment] %>