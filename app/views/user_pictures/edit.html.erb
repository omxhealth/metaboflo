<h1>Editing picture for user <%= @user.login %></h1>

<% form_for([@user, @user_picture], :html => { :multipart => true }) do |form| %>

  <%= render :partial => form %>

  <p>
    <%= form.submit "Update" %>
  </p>
<% end %>