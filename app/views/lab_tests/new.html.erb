<h1>Add Lab Test</h1>

<%= render_form_errors(@lab_test) %>
<%= knoxy_form_for([@test_subject, @lab_test]) do |form| %>
  <%= render :partial => 'form', :locals => { :form => form } %>

  <p>
    <%= form.submit "Create" %>
  </p>
<% end %>
