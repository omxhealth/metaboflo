<tr class="<%= cycle('even', 'odd') %>">
	<td><%= study_cohort_assignment.assignable %></td>
	<td><%= study_cohort_assignment.label %></td>
	<td><%= link_to "Remove", cohort_study_cohort_assignment_path(study_cohort_assignment.cohort, study_cohort_assignment), :remote => true, :method => :delete, :confirm => 'Are you sure?' %></td>
</tr>
